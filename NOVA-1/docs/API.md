# API

The HTTP API is introduced in Module 2. No routes exist in Module 0, avoiding a
non-functional placeholder contract. Every future endpoint will document its
request schema, response schema, authentication requirements, errors, and an
executable example.

